// Preencha com os dados do SEU projeto Supabase (Project Settings > API).
// Isso é seguro de deixar público: a chave "anon" só funciona dentro das
// regras (RLS) definidas em sql/schema.sql.

export const SUPABASE_URL = "https://SEU-PROJETO.supabase.co";
export const SUPABASE_ANON_KEY = "SUA-CHAVE-ANON-PUBLICA";

// Nome do bucket de storage usado para as fotos dos jet skis
export const PHOTOS_BUCKET = "jetski-photos";
